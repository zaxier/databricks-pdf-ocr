Running:
```
python -m databricks_pdf_ocr.sync upload data --catalog zaxier_dev --sch
ema pdf_parse --volume pdf_documents
```
for a volume that doesn't exist it just hangs.

Deal with spark session creation duplication.

PDF ingestion job finishing but not ingesting anything.

Issue: If I ctrl+c cancel an OCR run the state of the table gets stuck in 'processing' 